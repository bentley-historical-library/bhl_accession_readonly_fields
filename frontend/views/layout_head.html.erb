<% if controller.controller_name == "accessions" %>
  <%= javascript_include_tag "#{AppConfig[:frontend_prefix]}assets/accession_readonly_fields.js" %>

  <script>
     var init = function () {
        setTimeout(function () {
            new AccessionReadOnlyFields().init();
        }, 0);
     }

     $(document).bind("loadedrecordform.aspace", init);
     $(window).load(init);
 </script>
<% end %>


